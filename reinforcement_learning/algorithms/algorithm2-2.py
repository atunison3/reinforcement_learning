"""
Algorithm 2-2. Environment

function ENVIRONMENT(a, p(a))
intput: The action, a, and its probability, p(a)
output: r <- 1 with probability p(a), else 0
"""


def environment(a: int, p: float) -> int:
    """Determines the reward"""

    return 1
